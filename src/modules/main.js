const initialState = [
   {
    key:0,
    src:["../codeimg/memIndex.png"]
   }
];

export default function main ( state = initialState, action ){
    switch (action.type) {
        default:
            return state;
    }
}